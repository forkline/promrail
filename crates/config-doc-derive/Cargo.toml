[package]
name = "config-doc-derive"
version = "0.1.0"
edition = "2024"
description = "Derive macro for generating configuration documentation"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2", features = ["full", "parsing", "extra-traits"] }
quote = "1"
proc-macro2 = "1"
